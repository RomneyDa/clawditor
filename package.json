{
  "name": "clawlab",
  "version": "0.1.0",
  "type": "module",
  "description": "One-command OpenClaw beta and release-candidate validation launcher.",
  "repository": {
    "type": "git",
    "url": "https://github.com/RomneyDa/clawlab.git"
  },
  "license": "MIT",
  "preferGlobal": true,
  "files": [
    "dist/",
    "prompts/",
    "src/",
    "AGENTS.md",
    "README.md",
    "package.json"
  ],
  "bin": {
    "clawlab": "dist/clawlab.js"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "node -e \"require('node:fs').chmodSync('dist/clawlab.js', 0o755)\"",
    "test": "npm run build && node --test --import tsx test/*.test.ts",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
